@model Mirror.Models.ProxyCredential

@{
    ViewData["Header"] = "Настройки прокси";
}

<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="ListUrl" class="control-label"></label>
                <input asp-for="ListUrl" class="form-control" />
                <span asp-validation-for="ListUrl" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Применить изменения" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
